{
  "name": "shaoplatim-miniapp",
  "version": "0.1.0",
  "private": true,
  "description": "Telegram Mini App and admin panel for ЩаОплатим.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check public/admin.js && node --check api/orders.js && node --check api/orders/[id].js && node --check api/telegram-webhook.js && node --check api/health.js && node --check scripts/set-telegram-menu.js && node --check scripts/set-telegram-webhook.js && node scripts/check-trust-content.js && node scripts/check-api-security.js && node scripts/check-status-flow.js",
    "telegram:menu": "node scripts/set-telegram-menu.js",
    "telegram:webhook": "node scripts/set-telegram-webhook.js",
    "telegram:channel-post": "node scripts/send-channel-post.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2"
  },
  "engines": {
    "node": ">=20"
  }
}
